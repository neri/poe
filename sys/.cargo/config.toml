[build]
rustflags = ["-C", "relocation-model=static", "-C", "link-args=--image-base=0x0010000 --omagic"]
target = "i586-unknown-none.json"

[unstable]
build-std = ["core", "compiler_builtins", "alloc"]
build-std-features = ["compiler-builtins-mem"]
