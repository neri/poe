[package]
edition = "2024"
name = "poe-rv32"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
poe = { path = "../../poe/", features = ["virt", "device_tree"] }
minios = { path = "../../../minios/", features = ["virt", "minisbi", "device_tree"] }

[profile.release]
lto = true
opt-level = "z"
panic = "abort"
