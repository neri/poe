[package]
edition = "2024"
name = "poe-rv32"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
poe = { path = "../../poe/", features = ["sbi", "device_tree"] }
minios = { path = "../../../minios/", features = ["sbi", "device_tree"] }
minisbi = { path = "../../../lib/minisbi/" }

[profile.release]
lto = true
opt-level = "z"
panic = "abort"
